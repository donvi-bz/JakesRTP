forcertp:
  playerName !dynamic:
    -c:
      configName !dynamic:
    -c-w:
      configName !dynamic:
        worldName !dynamic:
    -w:
      worldName !dynamic:
rtp-admin:
  reload:
  reload-messages:
  status:
    "#static":
    configName !dynamic: